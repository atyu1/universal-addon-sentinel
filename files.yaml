common: # required, files present in every repo by default
  - addon.tf
  - variables-addon.tf
  - requirements.txt
  - versions.tf
  - LICENSE

# create additional custom groups, which are present in your repo
addon-irsa:
  - addon-irsa
  - variables-addon-irsa

addon-irsa-oidc:
  - addon-oidc
  - variables-addon-oidc
